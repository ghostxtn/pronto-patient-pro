$kind: http-request
name: Get Appointment
method: GET
url: '{{baseUrl}}/appointments/{{appointmentId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
